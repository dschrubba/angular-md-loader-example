# Angular Markdown Loader Example

A simple Angular project with a Markdown loader implementation. It reads .md files and renders them to HTML using [marked](https://github.com/markedjs/marked).
This can be used to store texts for static sites in Markdown files instead of using a database or writing the content directly into the templates.

Demo: https://dschrubba.github.io/angular-md-loader-example/
